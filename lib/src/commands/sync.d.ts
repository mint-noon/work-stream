import { Command } from 'commander';
import type { WatchOptions } from '../types';
export declare const sync: ({ watch, delay, }: WatchOptions) => void;
declare const _default: Command;
export default _default;
